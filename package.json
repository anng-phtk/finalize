{
  "name": "finalize",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start:app": "vite --host 0.0.0.0 --port 5713 --allowed-hosts all",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}
